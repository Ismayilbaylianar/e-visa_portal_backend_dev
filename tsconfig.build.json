{
  "extends": "./tsconfig.json",
  "comment": "prisma/seed.ts is a standalone dev bootstrap run via `prisma db seed` (its own ts-node), not part of the app runtime. It predates the entries feature (Stage 1+2) and needs reworking before re-seeding; excluded from the app build so it can't block `nest build`.",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "prisma/seed.ts"]
}
